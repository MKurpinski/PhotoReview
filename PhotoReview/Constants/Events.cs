namespace PhotoReview.Constants
{
    public static class Events
    {
        public const string PhotoApprovedEvent = nameof(PhotoApprovedEvent);
        public const string PhotoRejectedEvent = nameof(PhotoRejectedEvent);
    }
}
